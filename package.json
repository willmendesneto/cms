{
  "name": "cms",
  "version": "0.0.0",
  "repository": "https://github.com/movimento-sem-terra/cms",
  "devDependencies": {
    "http-server": "*",
    "bower": "*",
    "shelljs": "*"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000"
  }
}
